namespace BlackBox;

table LoginPayload {
    username: string (required);
}

union AnyPayload { LoginPayload }

table Message {
    type: byte;
    payload: AnyPayload (required);
}

root_type Message;
